######################################
# 🌐 Web (フロントエンド)
######################################

# Vite が開発時に呼び出す API のアドレス
# 開発環境: 通常は FastAPI ホストの 8000 ポート
# 本番環境: 通常は Traefik が /api パスをリバースプロキシする
VITE_API_URL=http://localhost:8000

# Vite の実行ポート（開発のみ）
VITE_PORT=5173


######################################
# ⚙️ API (バックエンド / FastAPI)
######################################

# Redis 接続（開発と本番で異なる点に注意）
# dev:   redis://localhost:6379/0
# prod:  redis://redis:6379/0 （コンテナ内部からのアクセス）
REDIS_URL=redis://localhost:6379/0

# バックエンドの暗号化 / JWT / Cookie 用のランダム文字列（本番では必ず変更）
SECRET_KEY=change-me

# 許可する CORS オリジン
# 複数指定する場合はカンマ区切り
# BACKEND_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
BACKEND_CORS_ORIGINS=http://localhost:5173

# API のリッスンポート（コンテナ内部では通常 8000）
API_PORT=8000


######################################
# 🧩 Optional Services (任意の連携)
######################################

# 例: 外部 AI / LLM 用 API キー（OpenAI, Azure など）
# OPENAI_API_KEY=sk-xxxxx
# AZURE_OPENAI_API_BASE=https://xxx.openai.azure.com/
# AZURE_OPENAI_API_VERSION=2024-05-01-preview

# ログレベル: debug / info / warning / error
LOG_LEVEL=info


######################################
# 🐳 Docker / Traefik (本番)
######################################

# Traefik のルーティング用ラベル（デプロイ時のみ使用）
TRAEFIK_ENTRYPOINT=web
TRAEFIK_HOST=reroute.localhost

# Nginx サービスのポートマッピング
WEB_PORT=80

# Redis の既定データベース
REDIS_DB=0
